[module/date]
type = internal/date
interval = 10.0

time = "%{T2} %{T-}%H:%M"
date-alt = "%{T2} %{T-}%A, %d %B %Y"

format = <label>
format-background = ${colors.moduleback}
format-foreground = ${colors.foreground}

label = %date%%time%
label-font = 1
